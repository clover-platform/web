import {keywords, title} from "@/utils/seo";

export const metadata = {
    title: title("{#登录#}"),
    keywords: keywords(),
}

const Page = () => {
    return <>login page</>
};

export default Page;
