import {keywords, title} from "@clover/public/utils/seo";

export const metadata = {
    title: title("{#动态#}"),
    keywords: keywords(),
}

const Page = () => <div>activity</div>

export default Page;
