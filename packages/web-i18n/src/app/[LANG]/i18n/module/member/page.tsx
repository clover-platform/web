import {keywords, title} from "@clover/public/utils/seo";

export const metadata = {
    title: title("{#成员#}"),
    keywords: keywords(),
}

const Page = () => <div>member</div>

export default Page;
