import {keywords, title} from "@clover/public/utils/seo";

export const metadata = {
    title: title("{#词条#}"),
    keywords: keywords(),
}

const Page = () => <div>source</div>

export default Page;
