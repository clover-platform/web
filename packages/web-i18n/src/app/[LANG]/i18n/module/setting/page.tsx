import {keywords, title} from "@clover/public/utils/seo";

export const metadata = {
    title: title("{#设置#}"),
    keywords: keywords(),
}

const Page = () => <div>setting</div>

export default Page;
