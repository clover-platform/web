import {keywords, title} from "@clover/public/utils/seo";

export const metadata = {
    title: title("{#翻译#}"),
    keywords: keywords(),
}

const Page = () => <div>translations</div>

export default Page;
