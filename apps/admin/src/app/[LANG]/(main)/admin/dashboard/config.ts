import { t } from '@easykit/common/utils/locale'

export const layoutConfig = {
    name: 'main',
    active: "overview.dashboard",
    path: [
        {
            title: t("管理中心"),
            type: "item",
        }
    ],
}
