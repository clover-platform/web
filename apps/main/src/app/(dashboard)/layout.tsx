'use client';
import {FC, PropsWithChildren} from "react";
import {DashboardLayout} from "@/components/layout/dashboard";

const Layout: FC<PropsWithChildren> = (props) => <DashboardLayout {...props} />
export default Layout;
