import {FC} from "react";

export type HeaderExtendProps = {
    active?: string;
}

export const HeaderExtend: FC<HeaderExtendProps> = (props) => {
    return <div>
        HeaderExtend
    </div>
}
