'use client'

import type { FC, PropsWithChildren } from 'react'
import { ModuleLayout } from '@/components/layout/module'

const Layout: FC<PropsWithChildren> = (props) => <ModuleLayout {...props} />
export default Layout
